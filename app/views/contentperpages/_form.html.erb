<%= form_for(@contentperpage) do |f| %>
  <% if @contentperpage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contentperpage.errors.count, "error") %> prohibited this contentperpage from being saved:</h2>

      <ul>
      <% @contentperpage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content_id %><br>
    <%= f.number_field :content_id %>
  </div>
  <div class="field">
    <%= f.label :menu_id %><br>
    <%= f.number_field :menu_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
